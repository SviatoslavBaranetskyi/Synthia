[project]
name = "training-service"
version = "0.1.0"

dependencies = [
    "torch",
    "transformers",
    "datasets",
    "evaluate",
    "scikit-learn",
    "numpy",
    "pandas",

    "core",
    "schemas"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "api-gateway"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",

    "core",
    "schemas",
    "utils"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
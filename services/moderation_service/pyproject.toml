[project]
name = "moderation-service"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",

    "torch",
    "transformers",
    "numpy",
    "scikit-learn",

    "core",
    "schemas"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"